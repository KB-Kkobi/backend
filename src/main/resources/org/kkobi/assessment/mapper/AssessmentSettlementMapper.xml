<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kkobi.assessment.mapper.AssessmentSettlementMapper">

    <insert id="saveProcessingAssessmentSettlement">
        INSERT INTO assessment_settlements (
            account_id,
            assessment_period_type,
            period_date,
            status
        )
        VALUES (
            #{accountId},
            #{assessmentPeriodType},
            #{periodDate},
            'PROCESSING'
        )
    </insert>

    <update id="restartStaleAssessmentSettlement">
        UPDATE assessment_settlements
        SET updated_at = CURRENT_TIMESTAMP
        WHERE account_id = #{accountId}
          AND assessment_period_type = #{assessmentPeriodType}
          AND period_date = #{periodDate}
          AND status = 'PROCESSING'
          AND updated_at &lt; #{staleBefore}
    </update>

    <update id="completeAssessmentSettlement">
        UPDATE assessment_settlements
        SET status = 'COMPLETED',
            completed_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE account_id = #{accountId}
          AND assessment_period_type = #{assessmentPeriodType}
          AND period_date = #{periodDate}
          AND status = 'PROCESSING'
    </update>

    <delete id="deleteProcessingAssessmentSettlement">
        DELETE FROM assessment_settlements
        WHERE account_id = #{accountId}
          AND assessment_period_type = #{assessmentPeriodType}
          AND period_date = #{periodDate}
          AND status = 'PROCESSING'
    </delete>

</mapper>
