<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kkobi.notification.mapper.NotificationMapper">

    <!-- 사용자 알림 설정 조회 -->
    <select id="findSettings"
            resultType="org.kkobi.notification.dto.response.NotificationSettingsResponseDto">
        SELECT
            trade_enabled AS tradeEnabled,
            friend_enabled AS friendEnabled
        FROM notification_settings
        WHERE user_id = #{userId}
    </select>


    <!-- 알림 설정 저장 / 기존 설정 수정 -->
    <insert id="upsertSettings">
        INSERT INTO notification_settings (
            user_id,
            trade_enabled,
            friend_enabled
        )
        VALUES (
                   #{userId},
                   #{tradeEnabled},
                   #{friendEnabled}
               )
        ON DUPLICATE KEY UPDATE
                             trade_enabled = #{tradeEnabled},
                             friend_enabled = #{friendEnabled},
                             updated_at = CURRENT_TIMESTAMP
    </insert>


    <!-- 사용자 알림 목록 최신순 조회 -->
    <select id="findNotifications"
            resultType="org.kkobi.notification.dto.response.NotificationResponseDto">
        SELECT
            notification_id AS notificationId,
            type,
            title,
            message,
            reference_id AS referenceId,
            is_read AS `read`,
            read_at AS readAt,
            created_at AS createdAt
        FROM notifications
        WHERE user_id = #{userId}
        ORDER BY created_at DESC, notification_id DESC
    </select>


    <!-- 읽지 않은 알림 개수 조회 -->
    <select id="countUnreadNotifications"
            resultType="int">
        SELECT COUNT(*)
        FROM notifications
        WHERE user_id = #{userId}
          AND is_read = FALSE
    </select>


    <!-- 개별 알림 읽음 처리 -->
    <update id="markAsRead">
        UPDATE notifications
        SET
            is_read = TRUE,
            read_at = CURRENT_TIMESTAMP
        WHERE notification_id = #{notificationId}
          AND user_id = #{userId}
          AND is_read = FALSE
    </update>


    <!-- 모든 알림 읽음 처리 -->
    <update id="markAllAsRead">
        UPDATE notifications
        SET
            is_read = TRUE,
            read_at = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
          AND is_read = FALSE
    </update>


    <!-- 신규 알림 저장 -->
    <insert id="insertNotification">
        INSERT INTO notifications (
            user_id,
            type,
            title,
            message,
            reference_id
        )
        VALUES (
                   #{userId},
                   #{type},
                   #{title},
                   #{message},
                   #{referenceId}
               )
    </insert>

    <!-- 현재 사용자의 모든 알림 삭제 -->
    <delete id="deleteAllNotifications">
        DELETE FROM notifications
        WHERE user_id = #{userId}
    </delete>

</mapper>