<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kkobi.account.mapper.AccountMapper">

    <!-- 해당 사용자의 계좌가 이미 존재하는지 확인 -->
    <select id="existsAccountByUserId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM accounts
            WHERE user_id = #{userId}
        )
    </select>

    <!-- 초기 투자금과 월 투자금을 반영하여 가상 계좌 생성 -->
    <insert id="saveAccount">
        INSERT INTO accounts (
            user_id,
            seed_money,
            monthly_invest_amount,
            cash_balance
        )
        VALUES (
                   #{userId},
                   #{request.seedMoney},
                   #{request.monthlyInvestAmount},
                   #{request.seedMoney}
               )
    </insert>

</mapper>